import { BetterAuthOptions } from "better-auth";
type Adv = BetterAuthOptions["advanced"];
// We just want to see what advanced or database options are there, but we can't easily reflect types at runtime.
